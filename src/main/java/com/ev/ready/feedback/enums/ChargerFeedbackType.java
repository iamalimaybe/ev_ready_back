package com.ev.ready.feedback.enums;

public enum ChargerFeedbackType {
    WORKING,
    NOT_WORKING,
    CONNECTOR_UNAVAILABLE,
    PRICE_CHANGED,
    ACCESS_ISSUE,
    LOCATION_WRONG,
    CLOSED_OR_REMOVED,
    OTHER
}
